# Hermes Agent 文件位置

Hermes Agent 已從 **App stack** taxonomy 移至 **AI stack**。完整架構、安全邊界、Docker control plane 與操作說明請參閱 [AI stack / Hermes Agent](../ai/hermes.md)。

此頁保留作為舊 App 導航的相容 stub；請使用 `docker-compose-ai.yml`，不要再以 `docker-compose-app.yml` 管理 Hermes。
